# use screen prefix keybinding
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# reload configuration
bind r source-file ~/.tmux.conf

# number windows beginning at 1
set -g base-index 1

# window splitting
unbind %
bind | split-window -h # horizontal split
unbind '"'
bind _ split-window -v # vertical split

# status line
set-option -g status-utf8 on
set-option -g status-justify left
set-option -g status-bg '#000000'
set-option -g status-fg white
set-option -g status-left-length 20
set-option -g status-right-length 45
set-window-option -g window-status-current-fg magenta

set -g status-left ''
# set -g status-left '#[fg=green][ session:#[fg=white]#S #[fg=green]][#[default]'

set -g status-right '#[fg=green][#[fg=white] #H #[fg=green]][ #[fg=white]%l:%M%P #[fg=blue]%m-%d-%Y #[fg=green]]#[default]'

